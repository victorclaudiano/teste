#5) Escreva um programa que inverta os caracteres de um string.


#IMPORTANTE:

#a) Essa string pode ser informada através de qualquer entrada de sua preferência ou pode ser previamente definida no código;

#b) Evite usar funções prontas, como, por exemplo, reverse;


def inverter_string(string):
    # Inicializamos uma string vazia para armazenar o resultado
    string_invertida = ""
    
    # Iteramos sobre a string de trás para frente e adicionamos cada caractere à string invertida
    for i in range(len(string) - 1, -1, -1):
        string_invertida += string[i]
    
    # Retornamos a string invertida
    return string_invertida

# String de exemplo
minha_string = "Olá, mundo!"

# Chamamos a função para inverter a string
string_invertida = inverter_string(minha_string)

# Exibimos o resultado
print("String original:", minha_string)
print("String invertida:", string_invertida)
